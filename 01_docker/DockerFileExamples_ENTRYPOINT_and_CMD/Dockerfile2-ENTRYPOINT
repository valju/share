FROM python:3.11
# --------------------------------------------

WORKDIR /app
COPY process_data.py requirements.txt ./
RUN pip install -r requirements.txt

# --------------------------------------------
ENTRYPOINT ["python", "process_data.py"]

# =>     python process_data.py      (runs always this whole command line)